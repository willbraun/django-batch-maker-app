// bring in step number as props from recipe input
// if last step, conditionally render an input form

const Step = ({}) => {
    // ingredients state as part of step?

    const [state, setState] = useState(blank);

    const stepInput = (

    )
    
    return (
        <section>

        </section>
    )
}

export default Step;